{
  "name": "alfa-browser-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Security-aware content gateway for autonomous agents",
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:mcp": "pnpm --filter mcp-server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
